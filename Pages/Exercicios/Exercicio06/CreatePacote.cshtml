@page
@model AgenciaTurismo.Pages.Exercicios.Exercicio06.CreatePacoteModel
@{
    ViewData["Title"] = "Exercício 06 - Cadastro de Pacote";
}

<h2 class="text-center mt-4">Cadastro de Pacote Turístico</h2>

<div class="d-flex justify-content-center">
    <div class="card p-4 mt-4" style="width: 400px;">
        <form method="post">
            <div class="mb-3">
                <label asp-for="Pacote.Titulo" class="form-label">Título</label>
                <input asp-for="Pacote.Titulo" class="form-control" />
                <span asp-validation-for="Pacote.Titulo" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Pacote.Preco" class="form-label">Preço</label>
                <input asp-for="Pacote.Preco" class="form-control" />
                <span asp-validation-for="Pacote.Preco" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary w-100">Cadastrar</button>
        </form>

        @if (Model.Mensagem != null)
        {
            <div class="alert alert-success mt-3 text-center">
                <strong>@Model.Mensagem</strong>
            </div>
        }
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
